---
batch_connect:
  template: "vnc"
script:
  job_environment:
    ANSYS_MODULE: <%= version %>
  native:
    resources:
      nodes: "<%= bc_num_slots.blank? ? "1" : bc_num_slots %><%= node_type %>"
